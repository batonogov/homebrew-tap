# Homebrew Tap for Pine

[Pine](https://github.com/batonogov/pine) — minimal native macOS code editor built with SwiftUI.

## Installation

```bash
brew tap batonogov/tap
brew install --cask pine
```

## Updating

```bash
brew upgrade --cask pine
```
